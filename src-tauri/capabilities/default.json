{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Teddy Mail desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "os:default",
    "updater:default",
    "process:default",
    "process:allow-restart"
  ]
}
